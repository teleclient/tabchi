===========(  تبچی ساز میدلاین )===========

در سایت cron-jobs.com ثبت نام کرده و اطلاعات آن را به همراه اطلاعات ربات خود

وارد فایل CFG.php کنید


یک کانال تاسیس کرده و ایدی و یروزنیم ان را داخل فایل CFG.php وارد کنید

ربات را بر رویه فایل index.php ست وبهوک کنید


داخل فایل index.php خط های 1283 فایل ایدی ویدیو ربات cli میدلاین را جایگزاری کنید

داخل فایل index.php خط 1276 نیز فایل ایدی ویدیوربات api را جایگزاری کنید


دقت داشته باشید که فایل های باید با همان رباطی دریافت شود که بر رویه ان تبچی ساز نصب میشود


بر رویه فایل CRJ.php نیز یک کرون جابز یک دقیقه ای ست کنید


















































